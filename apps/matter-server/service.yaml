apiVersion: v1
kind: Service
metadata:
  name: matter-server
spec:
  type: ClusterIP
  selector:
    app: matter-server
  ports:
    - name: ws
      port: 5580
      targetPort: 5580
      protocol: TCP
