apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-paperless-pvc
  namespace: paperless
spec:
  storageClassName: ""
  volumeName: nfs-paperless-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
