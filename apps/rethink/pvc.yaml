apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rethink-state-pvc
  namespace: homeassistant
spec:
  storageClassName: iscsi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
